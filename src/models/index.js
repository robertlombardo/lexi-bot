const DealershipModel   = require(`./dealership-model`)
const GameStateModel    = require(`./game-state-model`)
const UserModel         = require(`./user-model`)

module.exports = {
    DealershipModel,
    GameStateModel,
    UserModel,
}
